# Gabarito — Desafio 03 (Clientes)

Arquivos de referência para o mentor / revisão **depois** da tentativa do estagiário.

| Arquivo no app | Gabarito |
|----------------|----------|
| `lib/.../stores/cliente_page_store.dart` | `cliente_page_store_gabarito.dart` |
| `lib/.../cliente_page.dart` | `cliente_page_gabarito.dart` |

A fake API (`ClienteServices` + SQLite) já vem implementada no app — não precisa de gabarito.
